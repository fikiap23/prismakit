{
  "name": "@prismakit/example-fastify",
  "private": true,
  "version": "0.0.7",
  "description": "Minimal Fastify + PrismaKit example (no Docker / DB required)",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@prismakit/core": "workspace:*",
    "@prismakit/memory": "workspace:*",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
